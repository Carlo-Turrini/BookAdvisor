import { OverallRatingsForBook } from './overall-ratings-for-book';

describe('OverallRatingsForBook', () => {
  it('should create an instance', () => {
    expect(new OverallRatingsForBook(4,3, 4, 5)).toBeTruthy();
  });
});
